import {View} from 'react-native';

const Divider = ({width, height}) => {
  return (
    <View
      style={{
        width: width,
        height: height,
      }}
    />
  );
};

export default Divider;
